"""

"""
from logging import debug


def debug_print(request_item):

    debug("http response.url: {}".format(request_item.url))
    debug("http response.status_code: {}".format(request_item.status_code))
    debug("text response returned: {}".format(request_item.text))
